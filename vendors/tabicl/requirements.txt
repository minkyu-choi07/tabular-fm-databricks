# TabICL (soda-inria) vendor dependencies — self-hosted weights, runs locally on GPU.
# STUB — verify versions during implementation. See README.md.
tabicl
# tabicl[forecast]   # uncomment if implementing time-series forecasting
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
mlflow>=2.10.0
matplotlib>=3.7.0
