# Nori (Synthefy) vendor dependencies. The model is self-hosted and uses a GPU
# automatically when one is available; CPU is supported for smaller tables.
synthefy-nori==0.17.1
scikit-learn>=1.4.0
pandas>=2.0.0
numpy>=2.0.0
mlflow>=2.10.0
matplotlib>=3.7.0
