# Base / shared dependencies (data prep, common evaluation, comparison notebook).
# Vendor-specific dependencies live in vendors/<vendor>/requirements.txt.
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
mlflow>=2.10.0
matplotlib>=3.7.0
